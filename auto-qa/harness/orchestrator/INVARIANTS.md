# Cross-layer invariants — coverage index

_Auto-generated by `scripts/invariants-catalog.mjs`. Do not edit by hand;
run `npm run invariants:catalog` from `auto-qa/harness/` to regenerate
after adding or changing an invariant._

Each row is a single invariant in the orchestrator's catalog. The
`scenario-runner.mjs` runs them sequentially against the live stack;
this doc is the human-readable index of WHAT the harness asserts and
at WHICH layer.

Total invariants: **57** across **6** layers.

## Summary by layer

| Layer | Count |
|---|---|
| `api` | 10 |
| `api↔candles` | 5 |
| `api↔registry` | 3 |
| `orchestrator↔candles` | 21 |
| `orchestrator↔chain` | 10 |
| `orchestrator↔registry` | 8 |

## `api` (10)

| #  | Name | Description |
|----|------|-------------|
| 01 | `apiHealth` | api /health returns 200 |
| 02 | `apiHealthBodyShape` | api /health body is JSON {status: "ok", timestamp: <ISO 8601>} (catches refactor that drops fields, renames status, or changes timestamp format that ops dashboards parse) |
| 03 | `apiWarmer` | api /warmer returns 200 + JSON (warmer-status endpoint reachable) |
| 04 | `apiWarmerBodyShape` | api /warmer body conforms to getWarmerStatus() shape: {active, maxEntries, refreshIntervalSec, retentionDays} are non-negative finite numbers + entries is array (catches refactor that drops fields, renames, or changes numeric types to strings) |
| 05 | `apiSpotCandlesValidates` | api /api/v1/spot-candles (no ticker) returns 400 with JSON error (input validation alive) |
| 06 | `apiSpotCandlesHappyPath` | api /api/v1/spot-candles?ticker=… returns 200 + JSON with spotCandles array (data-plane reachable through validation → fetchSpotCandles → response transform) |
| 07 | `apiMarketEventsShape` | api /api/v1/market-events/proposals/:id/prices returns 200 + JSON with conditional_yes/no/spot/timeline structure (closes the 3-of-3 documented api endpoint coverage) |
| 08 | `apiUnifiedChartShape` | api /api/v2/proposals/:id/chart returns 200 + JSON with candles.{yes,no,spot} all arrays (data-plane reachable through proposal resolve → pool fetch → candle aggregation → response transform) |
| 09 | `apiUnifiedChartHasObservabilityHeaders` | api /api/v2/proposals/:id/chart sets X-Cache (HIT\|MISS) + X-Response-Time headers (catches ops-observability regressions invisible to body checks) |
| 10 | `apiUnifiedChartXCacheTtlPresent` | api /api/v2/proposals/:id/chart sets X-Cache-TTL to a positive integer string on BOTH HIT and MISS paths (catches refactor that drops TTL from one path or emits a non-numeric value) |

## `api↔candles` (5)

| #  | Name | Description |
|----|------|-------------|
| 01 | `chartCandleCountsBoundedByDirect` | sum of api unified-chart candles.{yes,no}.length ≤ direct candles indexer count (api filters by proposal pools so result is a strict subset) |
| 02 | `chartCandlesAreSubsetOfDirect` | every time value in api unified-chart candles.{yes,no} appears in the direct candles indexer time set (catches transform fabricating timestamps the indexer never emitted) |
| 03 | `apiCandlesGraphqlForwardsIntrospection` | api /candles/graphql forwards __schema introspection queries (sister to apiRegistryGraphqlForwardsIntrospection — completes the introspection-passthrough probe pair across both indexers) |
| 04 | `apiCanReachCandles` | api /candles/graphql proxies the __typename probe to candles checkpoint |
| 05 | `apiCandlesMatchesDirect` | latest Candle returned via api passthrough matches the one returned by direct indexer query (catches api caching drift, adapter rewriting, schema translation bugs) |

## `api↔registry` (3)

| #  | Name | Description |
|----|------|-------------|
| 01 | `apiCanReachRegistry` | api /registry/graphql proxies the __typename probe to registry checkpoint |
| 02 | `apiRegistryGraphqlForwardsIntrospection` | api /registry/graphql forwards __schema introspection queries (catches proxies that strip introspection at the api layer for security — silently breaks harness scenarios that depend on schema validation through api) |
| 03 | `apiRegistryMatchesDirect` | registry entities returned via api passthrough match direct indexer (proposalEntities + organizations + aggregators all checked in one query) |

## `orchestrator↔candles` (21)

| #  | Name | Description |
|----|------|-------------|
| 01 | `candlesDirect` | candles-checkpoint GraphQL responds to __typename without going through api |
| 02 | `candlesIndexerSchemaHasRequiredTypes` | candles indexer GraphQL __schema introspection lists Pool, Swap, and Candle types (catches schema-rename regressions invisible to data probes that hit the renamed-but-still-valid endpoint via fluke) |
| 03 | `candlesHasPools` | candles checkpoint has ≥1 Pool indexed |
| 04 | `poolTypeIsValidEnum` | all pools (first 50) have type ∈ {CONDITIONAL, PREDICTION, EXPECTED_VALUE} (catches schema drift, null types, and typos) |
| 05 | `candlesHasSwaps` | candles checkpoint has ≥1 Swap indexed (event-level sync verified) |
| 06 | `candlesHasCandles` | candles checkpoint has ≥1 Candle aggregated (period-aggregator alive) |
| 07 | `candleOHLCOrdering` | latest candle satisfies OHLC: low ≤ {open, close} ≤ high (vacuously true when no candles) |
| 08 | `candleOHLCAllRowsConsistent` | every candle (up to first 50) satisfies low ≤ {open, close} ≤ high (catches per-period min/max accumulator bugs that LATEST-only candleOHLCOrdering misses) |
| 09 | `swapAmountsPositive` | latest swap has amountIn > 0 AND amountOut > 0 (vacuously true when no swaps) |
| 10 | `swapAmountsBoundedAbove` | latest swap has amountIn < 1e15 AND amountOut < 1e15 (catches raw uint256 leaks where parseFloat returns 1e18 instead of a decimal-formatted string) |
| 11 | `swapAmountsAllRowsPositive` | every swap (up to first 50) has amountIn > 0 AND amountOut > 0 (catches partial-rewrite or per-block-decoder bugs that LATEST-only checks miss) |
| 12 | `swapTimestampSensible` | latest swap timestamp is in a sane range (catches event-topic-decoder bugs) |
| 13 | `candleTimeMonotonic` | recent candles are STRICTLY decreasing by time when ordered desc (catches duplicate-period or misordered candles) |
| 14 | `swapTimeMonotonicNonStrict` | recent swaps are non-strictly decreasing by timestamp when ordered desc (multiple swaps per block share a timestamp; going BACKWARDS is the bug) |
| 15 | `candleSwapTimeWindowConsistency` | latest swap.timestamp ≥ latest candle.time (catches future-period candle creation, stale swap data, time-field misalignment) |
| 16 | `candlePoolReferentialIntegrity` | latest Candle references a Pool that exists in the indexer (catches orphan candle aggregates from FK derivation bugs) |
| 17 | `swapPoolReferentialIntegrity` | latest swap references a Pool that exists in the indexer (catches orphan swap rows from FK derivation bugs) |
| 18 | `probabilityBounds` | for PREDICTION-type pools, latest candle close ∈ [0, 1] (the close IS the probability of YES; values outside this range are bugs) |
| 19 | `candlePricesNonNegative` | latest candle OHLC values are all ≥ 0 (universal sanity that catches all-negative or mixed-sign OHLC bugs the ordering check allows) |
| 20 | `candleVolumesNonNegative` | latest candle has volumeToken0 ≥ 0 AND volumeToken1 ≥ 0 (vacuously true when no candles) |
| 21 | `candleVolumesAllRowsNonNegative` | every candle (up to first 50) has volumeToken0 ≥ 0 AND volumeToken1 ≥ 0 (catches partial-rewrite or per-period-aggregator bugs that LATEST-only checks miss) |

## `orchestrator↔chain` (10)

| #  | Name | Description |
|----|------|-------------|
| 01 | `anvilBlockNumber` | eth_blockNumber returns a positive block number (chain has state) |
| 02 | `anvilClientVersionMentionsAnvil` | web3_clientVersion response contains "anvil" (case-insensitive) — pins the EVM client identity, not just chain ID |
| 03 | `anvilLatestBlockSensible` | eth_getBlockByNumber(latest) returns a block with a 0x… hash and a timestamp in [2020-01-01, now+1d] (catches stuck-clock + wrong-fork issues that the count-only block-number probe misses) |
| 04 | `anvilChainId` | eth_chainId returns 0x64 (chain 100, Gnosis) |
| 05 | `anvilGasPricePresent` | eth_gasPrice returns a 0x-prefixed positive hex value (catches EIP-1559-only mode + broken fee market that silently breaks gas estimation in scenarios) |
| 06 | `anvilNetworkVersionMatchesChainId` | net_version (decimal string) numerically equals eth_chainId (hex). Catches RPC handler regressions where the two methods diverge — silently breaks consumers that pick one or the other |
| 07 | `anvilImpersonationCapabilityPresent` | anvil_impersonateAccount RPC method works (catches "hardhat-compatible" or wrong-fork clients that identify as anvil in version string but lack the impersonation extension scenarios depend on) |
| 08 | `anvilSnapshotCapabilityPresent` | evm_snapshot RPC method works and returns a non-empty snapshot id (catches forks/clients lacking the snapshot extension scenarios depend on for state rollback between tests) |
| 09 | `anvilTimeWarpCapabilityPresent` | evm_setNextBlockTimestamp RPC method works — completes the chain CAPABILITY trio (impersonate + snapshot + time-warp) scenarios depend on for futarchy resolution flows that need to simulate time passing |
| 10 | `rateSanity` | sDAI getRate() returns a uint256 ≥ 1e18 (rate ≥ 1.0) |

## `orchestrator↔registry` (8)

| #  | Name | Description |
|----|------|-------------|
| 01 | `proposalEntityOrganizationReferentialIntegrity` | latest ProposalEntity references an Organization that exists in the registry indexer (closes the registry FK chain coverage) |
| 02 | `organizationAggregatorReferentialIntegrity` | latest Organization references an Aggregator that exists in the registry indexer (catches orphan-org from FK derivation bugs) |
| 03 | `registryDirect` | registry-checkpoint GraphQL responds to __typename without going through api |
| 04 | `registryIndexerSchemaHasRequiredTypes` | registry indexer GraphQL __schema introspection lists ProposalEntity, Organization, and Aggregator types (catches schema-rename regressions invisible to data probes that surface them as misleading "indexer empty" errors) |
| 05 | `registryHasProposalEntities` | registry checkpoint has ≥1 ProposalEntity indexed |
| 06 | `registryHasOrganizations` | registry checkpoint has ≥1 Organization indexed |
| 07 | `registryHasAggregators` | registry checkpoint has ≥1 Aggregator indexed |
| 08 | `registryHasFutarchyProdAggregator` | registry indexer has the production futarchy aggregator (0xc5eb43d53e2fe5fdde5faf400cc4167e5b5d4fc1, hardcoded in 3 api source files) |
